export interface CityResponse {
    city: string[];
  }
export interface CityAllAreaResponse {
    active: boolean;
    city: string;
    country: string;
    goodDoctorCoverageZoneId: string;
    postalCode: string;
    state: string;
  }
